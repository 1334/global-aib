<div class="slider-wrapper theme-default">
  <div id="slider" class="nivoSlider">
    <% @images = ["01.jpg", "02.jpg"] %>
    <% @images.each do |image| %>
      <%= image_tag (File.join @path, image), alt: "global-aib" %>
    <% end %>
  </div>
</div>
